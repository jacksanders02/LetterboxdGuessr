import React from 'react';
import { fontTitle } from '@/app/fonts';

export default function HowTo(): React.ReactNode {
  return (
    <main className={`flex flex-col ${fontTitle.className} items-center justify-center px-4 md:px-24`}>
      <h1>WIP</h1>
    </main>
  );
}
